# SQL artifacts (`pgsleuth--<version>.sql`) are generated by
# `cargo pgrx schema` / `cargo pgrx package` during the build.
# This directory is kept under version control so the layout is
# discoverable, but generated SQL files should not be committed —
# they are reproduced from src/lib.rs on every build.
